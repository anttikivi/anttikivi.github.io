---

---

<table class="about-table"><slot /></table>

<style is:global>
    .about-table {
        width: 100%;
        border-spacing: 0;
        border-collapse: collapse;
        text-align: left;
        border: 1px solid var(--gray);

        & thead {
            border-style: solid;
            border-width: 0 0 1px;
            background-color: var(--light-gray);
            border-color: var(--text);
        }

        & th,
        & td {
            padding: 0.125rem 0.5rem;
        }

        & td {
            border-bottom: 1px solid var(--gray);
        }

        & p {
            margin: 0.5rem 0;
        }

        & p:first-child {
            margin-top: 0;
        }

        & p:last-child {
            margin-bottom: 0;
        }
    }
</style>
